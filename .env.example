# opencode 用的环境变量模板 —— 复制为 .env 并填入真实 key
#
# 此文件只服务 opencode（opencode.json 里 {env:...} 占位符从这里取）。
# Python 脚本（test_api / gen_image / ...）凭据走 api.toml，不读 .env。

# 当前 opencode 主 LLM = sii-openai/gpt-5.5 → 需要 SII_API_KEY
# 在 https://apicz.boyuerichdata.com 获取
SII_API_KEY=sk-REPLACE_WITH_SII_KEY

# MiniMax provider 也声明在 opencode.json，未选中。若要切回 minimax-cn-coding-plan/MiniMax-M2.7-highspeed
# 需要 Token Plan Key（与 pay-as-you-go API Key 不通用），在 https://platform.minimax.io/ Billing 取
MINIMAX_API_KEY=REPLACE_WITH_TOKEN_PLAN_KEY
