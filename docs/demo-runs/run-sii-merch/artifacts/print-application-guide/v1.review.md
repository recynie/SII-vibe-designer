# Review · print-application-guide · v1

## 机器判定

### 字族（HTML 类硬门槛 / 其它 N/A）
- check_html_fonts: FAIL: 外来字族列表
  - spec fonts: montserrat-medium, sourcehansanscn-regular
  - html uses: montserrat-medium, source han sans cn, sourcehansanscn-regular
  - 'source han sans cn' not in spec 字体

**机器硬门槛结果：不通过**

---

## 实物观察

未进入实物观察（机器判定不过）。

---

## 主观打分

未进入主观打分（机器判定不过）。

---

## 判定

**不通过**

判定理由（2-3 行）：
机器判定发现 HTML 字族硬门槛失败：CSS 中使用了 `source han sans cn`，但 brand-spec 允许字体仅为 `montserrat-medium` 与 `sourcehansanscn-regular`。
按照评审流程，机器硬门槛不过时不进入实物观察和主观打分。

---

## 改进建议（仅在不通过时填）

按重要性排序，每条标根因层：

### 机器层
1. **[字族外来]** HTML 使用了 `source han sans cn`，spec 列的是 `sourcehansanscn-regular`；考虑让 designer 修复 CSS 中的 `font-family`，统一改为 brand-spec 允许的字体名。

### 主观层

无；机器硬门槛未通过，未进入主观评审。
